- title 'Edit media'

- if @asset.trashed?
  - breadcrumbs.add 'Trash', push_type.trashed_assets_path
- breadcrumbs.add @asset.description_or_file_name, push_type.edit_asset_path(@asset)

= render 'form'